# Flow schema-surface obstruction

The r4 parent attempt consumed two epistemics revisions on generic Flow type errors: field indices were passed after products, and `StateSchemaTypes` omitted structured contract values such as `Text(64)`. It returned typed `capacity_exceeded` before changing another semantic file.

The successor moves the complete policy-free contract type tuple into `release_contract.zig` and states the exact index-first Flow signatures. It contains no observation, admission, completion, or test behavior.
