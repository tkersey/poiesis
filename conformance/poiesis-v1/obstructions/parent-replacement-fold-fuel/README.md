# Parent replacement-fold fuel obstruction

Frozen Praxis v1.0.0 reached the r3 scaffold, passed baseline validation, authored and checked `generated_policy.zig`, then authored and applied `generated_epistemics.zig`. The second replacement was 6,809 bytes and its canonical result was 7,086 bytes, both well within the released parent's declared 16 KiB file/result profile.

While folding that retained replacement result, the parent emitted ten yielded Frames with an identical Machine-state digest. Its immutable runner failed closed before the required post-replacement check.

No Poiesis-local adapter, policy, child source, or host retry can increase the parent application's compiled per-step fuel. Repeating the tuple is excluded until an authorized Praxis successor proves the same fold or the normative birth profile is reduced.
