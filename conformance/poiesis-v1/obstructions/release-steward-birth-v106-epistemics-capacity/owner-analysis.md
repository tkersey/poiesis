# Owner analysis

Classification: `capability_obstruction`

Owner: Poiesis immutable scaffold lowering helpers.

Praxis v1.0.6 correctly admitted reads, checks, and five digest-bound
replacements. Agent Flow expressed the generated loops, the workspace adapter
enforced sequencing, and the model made progress after every fresh read. No
Agent, Boundary, World, host-lifecycle, or authority obstruction is established.

The current scaffold does not provide the generic lowering mechanics its own
16 KiB generated-source profile relies on: `working_set_helpers.zig` is 263
bytes and contains only typed text equality. Consequently the bounded semantic
slot owns both Release Steward laws and thousands of bytes of reusable Flow
plumbing. The smallest correction is to move only policy-neutral constants,
product replacement, keyed vector upsert, and bounded text-field scans into the
immutable helper and instruct generated source to call them.

This correction is falsified if a helper embeds a Release Steward type, field
index, action, capacity, sequencing rule, or completion predicate, or if a clean
successor birth again consumes the semantic slot by reimplementing these same
generic loops.
