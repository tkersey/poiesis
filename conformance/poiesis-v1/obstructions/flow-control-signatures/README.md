# Flow control-signature obstruction

The r8 birth used the complete schema tuple and reached four checked replacements, but generated epistemics still used incomplete generic Flow signatures: missing type arguments for constants/optionals and branch edges whose argument tuples did not match block parameters. The model returned typed `capacity_exceeded` before completing.

The successor guide adds only exact policy-free call shapes and block-edge laws from Agent v2.6.0.
