# Release Steward birth task failure

Four independent births using released Praxis v1.0.1 progressed without host or Machine stalls, applied three or four receiver-approved replacements, and then returned typed `capacity_exceeded`. The attempts repeatedly spent the six-replacement profile revising `generated_epistemics.zig` before completing definition and visible proof files.

The final r5 attempt consumed the corrected contract schema tuple and index-first Flow signatures, but its last parent-authored epistemics still copied observations unchanged and admitted every action. Publishing it would not establish Release Steward semantics even if the current shallow hidden identity tests passed.

This is a genuine task failure under the admitted six-replacement profile. No manual generated-source edit or hidden implementation was supplied.

After receiver-authorized corrections raised the bounded profile to 10 operations, aligned adapter admission, doubled total Machine fuel, and completed the generic Flow API surface, two Praxis v1.0.4 attempts still returned model-authored `capacity_exceeded` before completion. The remaining blocker is the selected model's ability to author the application within the now-realizable profile.
