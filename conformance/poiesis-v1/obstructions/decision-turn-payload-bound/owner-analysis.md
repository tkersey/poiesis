# Owner analysis

Classification: `application_closure_obstruction`

Owner: Poiesis child application profile at the World closure boundary.

Agent and World correctly enforce the declared maximum. The receiver-authored profile combined an exact 270,678-byte DecisionTurn with a 262,144-byte ceiling. Shrinking the DecisionView would violate the required Memory projection and repository capacities. The smallest observation-preserving correction is a 272 KiB ceiling on both sides of the same model effect.

The parent application, Agent, Boundary, World release, Machine ABI/state, Application ABI, Frame, host behavior, and Effect protocol do not change. The child application manifest and decision contract correctly record the successor bound.
